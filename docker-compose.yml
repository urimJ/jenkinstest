version: '3'
services:

  jinux2:
    image: ${DOCKER_USER_ID}/jenkinstest
    container_name: jinux2
    build:
       context: .

  production:
    image: ${DOCKER_USER_ID}/jenkinstest:${BUILD_NUMBER}
    restart: always
    ports:
      - 80:80



